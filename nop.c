#include "monty.h"

/**
 *nop - does nothing
 *@head: a doubly linked list
 *@line_number: line where call was made
 */

void nop(stack_t **head, int line_number)
{
	(void) head;
	(void) line_number;
}
